# MySQL backend (copy to web/.env and fill in your values)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=db_aq_gis
PORT=3001

# Optional: PostgreSQL/PostGIS via Docker (docker compose up -d)
# POSTGRES_DB=aq_gis
# POSTGRES_USER=aq_gis
# POSTGRES_PASSWORD=change_me
# POSTGRES_PORT=5432
